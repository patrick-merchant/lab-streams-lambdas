package org.examples.streamapi._5;

import org.examples.streamapi.TestUtil;
import org.examples.streamapi.model.Person;
import org.w3c.dom.ls.LSOutput;


public class SuperExtras {

    /*
        sort & comparator
     */

    //I am having some trouble with my imports - specifically, I can't get the sort() method from the COllections
    // interface, or the List interface. Attempted solution commented out below:

//    TestUtil.PEOPLE.sort((Person o1, Person o2)->o1.getAge().compareTo(o2.getAge()));


}
